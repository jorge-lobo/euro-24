<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.main.MainActivity">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.example.euro24.ui.main.MainViewModel" />

    </data>

    <RelativeLayout
        android:id="@+id/fullscreen"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal">

        <ImageView
            android:id="@+id/image_background"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:contentDescription="@string/description_image_background"
            android:scaleType="centerCrop"
            android:src="@drawable/background_main" />

        <include
            android:id="@+id/word_mark"
            layout="@layout/common_header_logo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/actual_date_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/word_mark"
            android:layout_alignParentStart="true"
            android:layout_marginTop="@dimen/margin_big"
            android:fontFamily="@font/teko_semi_bold"
            android:gravity="center_horizontal"
            android:textColor="@color/main_activity_text"
            android:textSize="@dimen/common_date"
            tools:text="Sunday, 5th January, 2024" />

        <RelativeLayout
            android:id="@+id/internet_connection_fragment_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/margin_normal"
            android:gravity="center">

        </RelativeLayout>

        <RelativeLayout
            android:id="@+id/tournament_fragment_container"
            android:layout_below="@id/actual_date_text"
            android:layout_width="match_parent"
            android:layout_marginBottom="64dp"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="@dimen/margin_normal"
            android:gravity="center">

        </RelativeLayout>

    </RelativeLayout>

</layout>